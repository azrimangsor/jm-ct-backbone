{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:dev": "cross-env NODE_ENV=dev npx playwright test --trace on --workers=1",
    "test:uat": "cross-env NODE_ENV=uat npx playwright test --trace on --workers=1",
    "test:prod": "cross-env NODE_ENV=production npx playwright test --trace on --workers=1",
    "test:ft-data-capture": "cross-env NODE_ENV=production npx playwright test --trace on --workers=1 --project=ct-dcf-ft --grep @smoke",
    "test:lch-data-capture": "cross-env NODE_ENV=production npx playwright test --trace on --workers=1 --project=ct-dcf-lch",
    "test:efuels-get-in-touch": "cross-env NODE_ENV=production npx playwright test --trace on --workers=1 --project=ct-gitf-efuels",
    "test:lch-get-in-touch": "cross-env NODE_ENV=production npx playwright test --trace on --workers=1 --project=ct-gitf-lch-estimator",
    "test:specific": "cross-env NODE_ENV=production npx playwright test lch-data-capture --trace on --workers=1 --grep @smoke",
    "test:generate-allure": "npx allure generate allure-results --clean -o allure-report",
    "test:open-allure": "npx allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.45.1",
    "@types/node": "^20.14.11",
    "allure-commandline": "^2.30.0",
    "allure-playwright": "^3.0.0-beta.10",
    "cross-env": "^7.0.3",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0",
    "jest": "^29.7.0",
    "jest-image-snapshot": "^6.4.0",
    "ts-node": "^10.9.2"
  }
}
